/**
 * @deprecated Replaced by the socket/ layer:
 *   - src/socket/SocketProvider.tsx  — connection lifecycle + context
 *   - src/socket/useGameState.ts     — server → client event listeners
 *   - src/socket/useGameActions.ts   — client → server emitters
 *   - src/socket/events.ts           — typed event names and payloads
 */
