import { createMuiTheme } from '@material-ui/core';

export const mainTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#31a24c',
    },
    secondary: {
      main: '#d12013',
    },
  },
});
